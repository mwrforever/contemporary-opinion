/// 本地私密配置模板 —— 复制为 `secrets.dart` 后填入你自己的值。
///
/// ⚠️ 安全：把 API Key 写进前端仅适合个人 Demo / 内测。
/// 生产环境应通过「后端 / 云函数代理」持有密钥，前端只拿短期 token。
///
/// 首次克隆仓库后：
///   1. 复制本文件：  cp lib/config/secrets.dart.example lib/config/secrets.dart
///   2. 把下面占位符替换为你的真实值（或改用环境变量注入，见下）。
///   3. `lib/config/secrets.dart` 已被 .gitignore 忽略，不会进版本库。
///
/// 也可用环境变量覆盖（无需改本文件）：
///   DASHSCOPE_API_KEY=sk-xxxx
///   DASHSCOPE_BASE_URL=https://llm-xxxx.maas.aliyuncs.com/api/v1
///   DASHSCOPE_SCHEDULE_MODEL=qwen3.7-max-2026-05-17
///   DASHSCOPE_ASR_MODEL=qwen3-asr-flash

const String kDashscopeApiKey = 'sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx';

const String kDashscopeBaseUrl =
    'https://llm-xxxxxxxxxxxx.maas.aliyuncs.com/api/v1';
